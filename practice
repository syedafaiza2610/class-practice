1. write a code where it calculate the length of a given array
2. for a given array [1,3,'a','b','f','a'] calculate how many time "a" tell the count of a
3. for agiven array find out if there is any element which is a number
4. print all names in the given list of students and greet good morning  name 
5. for a given array add all the elements and show the count [2,6,1] output 9
6. make an array of prices of shampoo,soap and facewash now increase every price by 10%
7. take temperature from user if the temperature is 25 or greater then print its too hot if temperature is less than 15 
 than its too cold if the temperature is 0 or less than so its freezing